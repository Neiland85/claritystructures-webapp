{
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "test": "npm run build:test && node --test dist/tests/*.spec.js",
    "build:test": "tsc -p tsconfig.typecheck.json",
    "demo:decision-engine": "node --input-type=module dist/scripts/demo-decision-engine.js"
  },
  "dependencies": {
    "nodemailer": "^8.0.1"
  }
}
