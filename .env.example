# ─── Database ────────────────────────────────────────────────────
DATABASE_URL="postgresql://user:password@localhost:5432/claritystructures"

# ─── SMTP (email notifications) ─────────────────────────────────
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="noreply@example.com"
SMTP_PASS="your-smtp-password"
SMTP_FROM="noreply@example.com"

# ─── Auth & Security ────────────────────────────────────────────
# Must be at least 32 characters. Used for bearer token verification.
JWT_SECRET="replace-with-at-least-32-char-secret"
SESSION_SECRET="replace-with-at-least-32-char-session-secret"

# Optional: separate admin API token (falls back to JWT_SECRET)
# ADMIN_API_TOKEN="replace-with-at-least-32-char-admin-token"

# ─── Rate Limiting (optional — falls back to in-memory) ─────────
# UPSTASH_REDIS_REST_URL="https://your-instance.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# ─── Observability (optional) ───────────────────────────────────
# SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"

# ─── Client-Side ────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME="ClarityStructures"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: PostHog analytics (only active when user grants cookie consent)
# NEXT_PUBLIC_POSTHOG_KEY="phc_your_posthog_key"

# ─── Build Flags ────────────────────────────────────────────────
# Set to "true" to skip env validation during build (CI/CD only)
# SKIP_ENV_VALIDATION=true
NODE_ENV="development"
