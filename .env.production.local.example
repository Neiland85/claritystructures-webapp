# ─── PRODUCCIÓN - Variables Requeridas ─────────────────────────────
# Copiar este archivo como .env.production.local y rellenar valores

# ─── Database (Supabase PostgreSQL) ─────────────────────────────
DATABASE_URL="postgresql://user:password@host:5432/claritystructures"

# ─── SMTP (Email Notifications) ─────────────────────────────────
SMTP_HOST="smtp.resend.com"
SMTP_PORT=587
SMTP_USER="resend"
SMTP_PASS="your-resend-api-key"
SMTP_FROM="noreply@claritystructures.com"

# ─── Auth & Security ────────────────────────────────────────────
# CRÍTICO: Mínimo 32 caracteres, caracteres aleatorios
JWT_SECRET="your-32-char-minimum-random-jwt-secret-here"
SESSION_SECRET="your-32-char-minimum-random-session-secret"
CRON_SECRET="your-32-char-minimum-random-cron-secret"

# Opcional: Token separado para admin API (fallback a JWT_SECRET)
# ADMIN_API_TOKEN="your-admin-api-token-here"

# ─── Rate Limiting (Upstash Redis) ─────────────────────────────
# UPSTASH_REDIS_REST_URL="https://your-instance.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# ─── Observability ─────────────────────────────────────────────
# SENTRY_DSN="https://key@project.ingest.sentry.io/number"

# ─── Client-Side URLs ──────────────────────────────────────────
NEXT_PUBLIC_APP_NAME="ClarityStructures"
NEXT_PUBLIC_APP_URL="https://your-domain.com"

# Opcional: Analytics (PostHog)
# NEXT_PUBLIC_POSTHOG_KEY="phc_your_key"
# NEXT_PUBLIC_POSTHOG_HOST="https://your-posthog-host.com"

# ─── Build ─────────────────────────────────────────────────────
NODE_ENV="production"
NEXT_TELEMETRY_DISABLED="1"
